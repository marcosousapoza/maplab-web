# Agent Boundaries

Work only in `maplab-web`. Consume the documented `/api` and generated WASM
contracts without inspecting sibling implementations. File a cross-repository issue
when another repository owns a change. Run check, tests, and build before completion.
